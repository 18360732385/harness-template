---
description: API 文档同步规则
globs:
alwaysApply: true
---

# API Doc Sync Rules

## 权威来源说明

- 本文件为 API 文档同步规则权威定义。
- `harness-collab/05-methodology/05-rules/api-doc-sync-rules.md` 为同步副本。

## 变更触发条件
以下任一变更发生，必须同步更新接口文档：
- 入参结构变更
- 返回参数变更
- URL 地址变更
- 请求方式变更

## 文档格式约束
- 使用统一模板：`harness-collab/04-api-docs/templates/api-doc-template.md`
- 具体接口说明文件：`harness-collab/04-api-docs/modules/` 下按业务模块新增或更新 `.md`
- 必须包含：接口功能、业务流程、请求参数、响应参数。
- `data` 为对象时，必须完整展开所有子字段：`data.xxx`、`data.obj.xxx`。

## 判定清单（满足任一项即必须更新）

- 新增接口。
- 修改请求参数字段、类型、是否必填、默认值。
- 修改响应结构字段、类型、语义、错误码。
- 修改 URL、请求方式、鉴权方式、限流策略。
- 修改接口业务语义（即使参数未变）。

## 与功能资产联动规则

- 接口新增/下线时，必须同步更新 `harness-collab/func.md`（或团队约定的 func 权威路径）对应服务能力描述。
- 接口语义变更时，必须同步更新上述 func 的方法说明与调用方影响。

## 门禁要求
- 文档不同步视为交付不完整，禁止进入发布流程。
- 变更说明中必须包含“API 文档已同步”和“harness-collab/func.md（或团队约定的 func 权威路径）已同步”的核对结果。
