---
description: 测试与质量门禁规则
globs:
alwaysApply: true
---

# Testing And Quality Rules

## 权威来源说明

- 本文件为测试与质量规则权威定义。
- `harness-collab/methodology/05-rules/testing-and-quality-rules.md` 为同步副本。

## 触发条件

- 任何代码变更（功能、修复、重构、配置）都必须执行本规则。

## 测试策略
- 单元测试：覆盖正常路径、边界路径、异常路径。
- 集成测试：覆盖核心业务链路和外部依赖交互。
- 回归测试：缺陷修复必须有对应回归用例。

## 质量门禁策略
- `observe`：采集数据，不阻塞。
- `warn`：告警并纳入治理清单。
- `enforce`：不达标即阻塞合入。

## 最低交付门槛
- `mvn clean verify` 通过（统一门禁命令）。
- Checkstyle 与 SpotBugs 无阻塞问题。
- 分层与依赖方向符合 `harness-collab/methodology/01-architecture/architecture-constraints.md`，经评审与治理闭环落实（本模板不内置自动结构测试；如需可自行引入 ArchUnit）。
- JaCoCo 覆盖率检查完成（默认模板为 `warn` 语义，需按项目阶段切换为 `enforce`）。
- API 文档与 `harness-collab/func.md` 已同步。

## 分档执行命令

- 历史项目（legacy）：`mvn clean verify -Pharness-legacy`
- 新项目（new）：`mvn clean verify -Pharness-new`
- 安全扫描（可选）：`mvn clean verify -Psecurity-scan`

## 触发场景建议

- `legacy`：存量系统接入初期，先观察与逐步收敛。
- `new`：新项目或治理完成后，直接 enforce。

## 验收证据格式

- 测试报告：`target/surefire-reports/*`
- SpotBugs 报告：`target/spotbugsXml.xml`
- JaCoCo 报告：`target/site/jacoco/*`
- 命令记录：执行命令与结果摘要需记录在执行计划或变更说明中。
