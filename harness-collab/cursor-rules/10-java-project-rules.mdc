---
description: Java 项目工程与编码规则（Spring Boot + Maven）
globs: "**/*.java"
alwaysApply: false
---

# Java Project Rules

## 权威来源说明

- 本文件为 Java Project Rule 权威定义。
- `harness-collab/methodology/05-rules/java-project-rules.md` 必须与本文件保持同步。

## 触发条件

- 新增或修改 Java 代码。
- 涉及分层边界、异常处理、数据对象、事务控制、幂等等场景。

## 架构分层
- `interfaces`：控制器与入站适配。
- `application`：业务编排与用例。
- `domain`：核心业务模型与规则。
- `infrastructure`：数据库、MQ、外部 API 适配。

## 依赖约束
- 允许：`interfaces -> application -> domain`
- 允许：`infrastructure -> domain`
- 禁止：`application -> interfaces`
- 禁止：`domain -> infrastructure`

## 依赖约束示例

- 允许示例：`OrderController -> OrderAppService -> OrderDomainService`
- 禁止示例：`OrderAppService -> OrderController`
- 禁止示例：`OrderDomainService -> OrderRepositoryImpl`

## 编码规范
- 严格遵守阿里巴巴《Java 开发手册》。
- 优先使用语义化枚举，不使用魔法值。
- Controller 不承载业务逻辑，Service 保持单一职责。
- 统一异常模型，返回可读错误码与错误信息。
- DTO/VO 与领域模型职责分离，避免相互污染。
- 涉及幂等场景时必须明确幂等键与去重策略。
- 涉及事务时必须明确事务边界与传播行为。

## 注释规范（强制）
- 每个类必须包含：职责、作者、创建时间。
- 每个方法必须包含：功能、入参、出参、异常说明。
- 方法超过 5 行时，关键业务块必须有中文注释说明“为什么这样做”。

## 可测试性约束
- 业务核心必须有单元测试。
- 跨模块流程必须有集成测试。
- 每个缺陷修复必须补充回归测试。

## 输出与验收

- 输出：代码改动说明、影响范围、测试证据、文档同步记录。
- 验收：`mvn clean verify` 通过，且无分层违规与关键规范违规。
