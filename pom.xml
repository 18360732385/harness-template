<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.4</version>
        <relativePath/>
    </parent>

    <groupId>com.example</groupId>
    <artifactId>harness-template</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <name>harness-template</name>
    <description>Reusable Java Harness Engineering scaffold</description>
    <packaging>jar</packaging>

    <properties>
        <java.version>17</java.version>
        <harness.mode>legacy</harness.mode>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <archunit.version>1.3.0</archunit.version>
        <archunit.allow.empty.should>true</archunit.allow.empty.should>
        <archunit.application.infrastructure.allowed>true</archunit.application.infrastructure.allowed>
        <archunit.enforce.infrastructure.boundary>false</archunit.enforce.infrastructure.boundary>
        <jacoco.coverage.minimum>0.60</jacoco.coverage.minimum>
        <jacoco.haltOnFailure>false</jacoco.haltOnFailure>
        <checkstyle.config.file>config/checkstyle/checkstyle.xml</checkstyle.config.file>
        <spring-boot.repackage.skip>true</spring-boot.repackage.skip>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>${archunit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>${spring-boot.repackage.skip}</skip>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <useModulePath>false</useModulePath>
                    <systemPropertyVariables>
                        <harness.mode>${harness.mode}</harness.mode>
                        <archunit.allow.empty.should>${archunit.allow.empty.should}</archunit.allow.empty.should>
                        <archunit.application.infrastructure.allowed>${archunit.application.infrastructure.allowed}</archunit.application.infrastructure.allowed>
                        <archunit.enforce.infrastructure.boundary>${archunit.enforce.infrastructure.boundary}</archunit.enforce.infrastructure.boundary>
                    </systemPropertyVariables>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>enforce-runtime</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireMavenVersion>
                                    <version>[3.9.0,)</version>
                                </requireMavenVersion>
                                <requireJavaVersion>
                                    <version>[17,)</version>
                                </requireJavaVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>3.3.1</version>
                <configuration>
                    <configLocation>${checkstyle.config.file}</configLocation>
                    <encoding>UTF-8</encoding>
                    <consoleOutput>true</consoleOutput>
                    <failsOnError>true</failsOnError>
                </configuration>
                <executions>
                    <execution>
                        <id>checkstyle-verify</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <version>4.8.6.6</version>
                <configuration>
                    <excludeFilterFile>config/spotbugs/exclude.xml</excludeFilterFile>
                </configuration>
                <executions>
                    <execution>
                        <id>spotbugs-verify</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.12</version>
                <executions>
                    <execution>
                        <id>jacoco-prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>jacoco-report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>jacoco-check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <haltOnFailure>${jacoco.haltOnFailure}</haltOnFailure>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.coverage.minimum}</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>harness-legacy</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <harness.mode>legacy</harness.mode>
                <checkstyle.config.file>config/checkstyle/checkstyle.xml</checkstyle.config.file>
                <jacoco.coverage.minimum>0.60</jacoco.coverage.minimum>
                <jacoco.haltOnFailure>false</jacoco.haltOnFailure>
                <archunit.allow.empty.should>true</archunit.allow.empty.should>
                <archunit.application.infrastructure.allowed>true</archunit.application.infrastructure.allowed>
                <archunit.enforce.infrastructure.boundary>false</archunit.enforce.infrastructure.boundary>
            </properties>
        </profile>
        <profile>
            <id>harness-new</id>
            <properties>
                <harness.mode>new</harness.mode>
                <checkstyle.config.file>config/checkstyle/checkstyle-strict.xml</checkstyle.config.file>
                <jacoco.coverage.minimum>0.80</jacoco.coverage.minimum>
                <jacoco.haltOnFailure>true</jacoco.haltOnFailure>
                <archunit.allow.empty.should>false</archunit.allow.empty.should>
                <archunit.application.infrastructure.allowed>false</archunit.application.infrastructure.allowed>
                <archunit.enforce.infrastructure.boundary>true</archunit.enforce.infrastructure.boundary>
            </properties>
        </profile>
        <profile>
            <id>security-scan</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.owasp</groupId>
                        <artifactId>dependency-check-maven</artifactId>
                        <version>10.0.4</version>
                        <configuration>
                            <format>HTML</format>
                            <failBuildOnCVSS>11</failBuildOnCVSS>
                        </configuration>
                        <executions>
                            <execution>
                                <id>dependency-check</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>check</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
