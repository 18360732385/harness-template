---
description: Harness 用户协作规则（Java 通用）
globs:
alwaysApply: true
---

# 用户协作规则

## 权威来源说明

- 本目录（`.cursor/rules`）是规则权威来源。
- `docs/05-rules` 是可复制的文档副本，变更时需同步更新。

## 目标
- 让 AI 深度参与需求理解、设计、计划、编码、测试、修复全流程。
- 保证输出可追溯、可验证、可复用。

## 触发条件

- 接收到需求实现、缺陷修复、重构优化、历史项目改造任务。
- 任何任务涉及代码变更、接口变更或业务流程调整。

## 任务分型与必需输出

- 新功能：`product-specs` + `design-docs` + `exec-plans` + 测试 + 文档同步。
- 改造/重构：改造说明 + 影响分析 + `exec-plans` + 回滚策略 + 测试。
- 缺陷修复：问题复盘 + 修复方案 + 回归用例 + 文档同步。

## 强制执行流程

1. 开发前必须先读取 `func.md`，评估复用。
2. 先产出 `product-specs`，再产出 `design-docs`，再产出 `exec-plans`。
3. 严格执行 plan_vs_impl；计划未覆盖的实现不得直接落地。
4. 所有关键改动必须补齐测试与文档。

## 提问与澄清策略

- 仅在关键歧义点发问，优先给默认可执行方案。
- 澄清问题最多 1-2 个，且必须影响实施路径。
- 大任务拆成原子步骤，确保每步可验证。

## 输出物要求

- 需求：`product-specs/*`
- 设计：`design-docs/*`
- 计划：`exec-plans/*`
- 实现与测试：`src/main/java/*`、`src/test/java/*`
- 文档同步：`docs/*`、`func.md`

## 验收证据（DoD）

- 设计/计划/实现三者映射关系清晰。
- 本地验证命令通过：`mvn clean verify`
- API 文档与 `func.md` 同步完成。
- 输出变更说明，明确影响范围与回滚策略。

## 新旧项目复用策略
- 新项目：默认 `warn`，首个迭代后切 `enforce`。
- 历史项目：`observe -> warn -> enforce` 渐进启用。
